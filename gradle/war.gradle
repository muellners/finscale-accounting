apply plugin: "war"

bootWar {
    mainClassName = "org.muellners.finscale.accounting.AccountingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
